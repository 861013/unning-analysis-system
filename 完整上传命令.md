# 完整上传命令步骤

本文档提供从终端上传项目到GitHub的完整命令步骤。

**仓库地址**: https://github.com/861013/unning-analysis-system

---

## 方式一：使用自动化脚本（推荐）

### 执行脚本

```bash
cd /home/hadoop/nosql/run/demo
./上传到GitHub.sh
```

脚本会自动执行所有步骤，包括：
- 初始化Git仓库
- 添加文件
- 提交到本地
- 连接远程仓库
- 推送到GitHub

---

## 方式二：手动执行命令

### 步骤1：进入项目目录

```bash
cd /home/hadoop/nosql/run/demo
```

### 步骤2：初始化Git仓库

```bash
git init
```

### 步骤3：添加所有文件到暂存区

```bash
git add .
```

**查看将要提交的文件：**
```bash
git status
```

### 步骤4：提交到本地仓库

```bash
git commit -m "初始提交：跑步分析系统项目

- 完整的项目代码（后端、前端、小程序）
- 项目文档（功能总结、启动指南等）
- 技术栈：Python + FastAPI + MongoDB
- 功能：数据采集、分析、可视化、训练计划生成"
```

### 步骤5：添加远程仓库

```bash
git remote add origin https://github.com/861013/unning-analysis-system.git
```

**验证远程仓库地址：**
```bash
git remote -v
```

应该显示：
```
origin  https://github.com/861013/unning-analysis-system.git (fetch)
origin  https://github.com/861013/unning-analysis-system.git (push)
```

### 步骤6：设置主分支为main

```bash
git branch -M main
```

### 步骤7：推送到GitHub

```bash
git push -u origin main
```

---

## 身份验证

如果推送时提示输入用户名和密码：

### 用户名
```
861013
```

### 密码
**使用Personal Access Token（不是GitHub登录密码）**

#### 如何创建Personal Access Token：

1. 访问 https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 填写信息：
   - **Note**: 例如 `本地开发`
   - **Expiration**: 选择过期时间（建议90天或更长）
   - **Select scopes**: 至少勾选 `repo`（完整仓库访问权限）
4. 点击 **"Generate token"**
5. **立即复制生成的token**（只显示一次，无法再次查看）
6. 在Git推送时，密码处粘贴这个token

---

## 完整命令序列（复制粘贴）

```bash
# 进入项目目录
cd /home/hadoop/nosql/run/demo

# 初始化Git仓库
git init

# 添加所有文件
git add .

# 提交到本地仓库
git commit -m "初始提交：跑步分析系统项目

- 完整的项目代码（后端、前端、小程序）
- 项目文档（功能总结、启动指南等）
- 技术栈：Python + FastAPI + MongoDB
- 功能：数据采集、分析、可视化、训练计划生成"

# 添加远程仓库
git remote add origin https://github.com/861013/unning-analysis-system.git

# 设置主分支为main
git branch -M main

# 推送到GitHub
git push -u origin main
```

---

## 常见问题

### 问题1：提示"remote origin already exists"

**解决方案：**
```bash
# 删除旧的远程仓库
git remote remove origin

# 重新添加
git remote add origin https://github.com/861013/unning-analysis-system.git
```

或者直接更新：
```bash
git remote set-url origin https://github.com/861013/unning-analysis-system.git
```

### 问题2：提示"failed to push some refs"

**可能原因：** 远程仓库有本地没有的提交（例如在GitHub网页上创建了README文件）

**解决方案：**
```bash
# 先拉取远程更改
git pull origin main --allow-unrelated-histories

# 解决可能的冲突后，再推送
git push -u origin main
```

### 问题3：提示"Support for password authentication was removed"

**原因：** GitHub不再支持密码认证，需要使用Personal Access Token

**解决方案：** 按照上面的步骤创建Personal Access Token，然后在密码处使用token

### 问题4：忘记添加.gitignore，提交了不应该提交的文件

**解决方案：**
```bash
# 从Git中删除文件（但保留本地文件）
git rm --cached 文件名

# 更新.gitignore文件
# 然后重新提交
git add .gitignore
git commit -m "更新.gitignore，移除不应提交的文件"
git push
```

---

## 验证上传成功

上传成功后，访问以下地址查看您的代码：

**仓库地址**: https://github.com/861013/unning-analysis-system

您应该能看到：
- ✅ 所有项目文件
- ✅ README.md文件
- ✅ 项目结构
- ✅ 提交历史

---

## 后续更新

以后修改代码后，使用以下命令更新：

```bash
cd /home/hadoop/nosql/run/demo

# 添加修改的文件
git add .

# 提交修改
git commit -m "更新说明：描述本次修改的内容"

# 推送到GitHub
git push
```

---

## 快速检查清单

- [ ] 已进入demo目录
- [ ] Git仓库已初始化
- [ ] .gitignore文件已存在
- [ ] 所有文件已添加到暂存区
- [ ] 已提交到本地仓库
- [ ] 远程仓库地址已添加
- [ ] Personal Access Token已创建（如果需要）
- [ ] 已成功推送到GitHub
- [ ] 可以在GitHub网页上查看代码

---

**祝您上传成功！** 🎉

